# Test Environment Configuration
APP_ENV=test
APP_DEBUG=false

# Server Configuration
SERVER_HOST=localhost
SERVER_PORT=8081

# Database Configuration (for testing)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=assignment-db-test
DB_SSL_MODE=disable
DB_MAX_OPEN_CONNS=5
DB_MAX_IDLE_CONNS=2

# Logging Configuration (minimal for testing)
LOG_LEVEL=error
LOG_FORMAT=json
LOG_OUTPUT=stderr

# Disable features not needed in tests
CORS_ENABLED=false
RATE_LIMIT_ENABLED=false